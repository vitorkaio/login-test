import React from 'react';
import { View } from 'react-native';
import Icon from 'react-native-vector-icons/MaterialIcons';

const Login = () => {
  return (
    <View>
      <Icon name="person" size={40} color="#8a05be" />
    </View>
  );
};

export default Login;
